<h1>Take a look at out flats!</h1>

<ul>
  <% @flats.each do |flat| %>
    <li>
    <%= link_to flat['name'], flat_path(flat['id']) %> -> <%= flat['price'] %> <%= flat['priceCurrency'] %>
    </li>
  <% end %>
</ul>

